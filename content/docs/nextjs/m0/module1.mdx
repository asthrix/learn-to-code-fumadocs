---
title: Module 1 - React Setup & First Component
description:
   Learn how to set up a React development environment and create your first
   React component
---

# Module 1: React Setup & First Component

Welcome to your React journey! In this module, you'll learn how to set up a
React development environment and create your first React component.

## Prerequisites

Before we start, make sure you have:

-  **Node.js** (version 14 or higher) installed
-  **npm** or **yarn** package manager
-  A code editor (VS Code recommended)
-  Basic knowledge of JavaScript, HTML, and CSS

## Setting Up Your Development Environment

### Option 1: Create React App (Recommended for Beginners)

The easiest way to get started with React is using Create React App:

```bash
npx create-react-app my-first-react-app
cd my-first-react-app
npm start
```

### Option 2: Vite (Modern Alternative)

For a faster development experience, you can use Vite:

```bash
npm create vite@latest my-react-app -- --template react
cd my-react-app
npm install
npm run dev
```

## Understanding the Project Structure

After creating your project, you'll see this structure:

```
my-react-app/
├── public/
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── App.js
│   ├── App.css
│   ├── index.js
│   └── index.css
├── package.json
└── README.md
```

### Key Files Explained

-  **`src/index.js`**: Entry point of your React application
-  **`src/App.js`**: Main App component
-  **`public/index.html`**: HTML template
-  **`package.json`**: Project dependencies and scripts

## Your First React Component

Let's examine the default `App.js` component:

```jsx
// src/App.js
import React from "react";
import "./App.css";

function App() {
   return (
      <div className='App'>
         <header className='App-header'>
            <h1>Welcome to React!</h1>
            <p>
               Edit <code>src/App.js</code> and save to reload.
            </p>
         </header>
      </div>
   );
}

export default App;
```

## Understanding JSX

JSX is a syntax extension for JavaScript that looks similar to HTML. Here are
the key differences:

```jsx
// JSX Example
function Welcome() {
   const name = "React Developer";

   return (
      <div className='welcome-container'>
         <h1>Hello, {name}!</h1>
         <img src='/logo.png' alt='React Logo' />
         <button onClick={() => alert("Hello!")}>Click me!</button>
      </div>
   );
}
```

### JSX Rules to Remember

1. **className instead of class**: Use `className` for CSS classes
2. **camelCase for attributes**: `onClick` instead of `onclick`
3. **Self-closing tags**: `<img />`, `<br />`, `<input />`
4. **JavaScript expressions**: Use `{}` to embed JavaScript

## Creating Your First Custom Component

Let's create a simple greeting component:

```jsx
// src/components/Greeting.js
import React from "react";

function Greeting({ name, age }) {
   return (
      <div
         style={{
            padding: "20px",
            border: "2px solid #4CAF50",
            borderRadius: "8px",
            margin: "10px 0",
         }}
      >
         <h2>Hello, {name}!</h2>
         <p>You are {age} years old.</p>
         <p>Welcome to React!</p>
      </div>
   );
}

export default Greeting;
```

Now use it in your `App.js`:

```jsx
// src/App.js
import React from "react";
import Greeting from "./components/Greeting";
import "./App.css";

function App() {
   return (
      <div className='App'>
         <header className='App-header'>
            <h1>My React Application</h1>
            <Greeting name='Alice' age={25} />
            <Greeting name='Bob' age={30} />
            <Greeting name='Charlie' age={22} />
         </header>
      </div>
   );
}

export default App;
```

## Hands-On Exercise: Personal Card Component

Create a `PersonalCard` component that displays personal information:

```jsx
// src/components/PersonalCard.js
import React from "react";

function PersonalCard({ name, occupation, location, hobbies, avatar }) {
   return (
      <div
         style={{
            maxWidth: "300px",
            margin: "20px auto",
            padding: "20px",
            border: "1px solid #ddd",
            borderRadius: "12px",
            boxShadow: "0 4px 6px rgba(0, 0, 0, 0.1)",
            textAlign: "center",
            backgroundColor: "#fff",
         }}
      >
         <img
            src={avatar || "https://via.placeholder.com/100"}
            alt={`${name}'s avatar`}
            style={{
               width: "100px",
               height: "100px",
               borderRadius: "50%",
               marginBottom: "15px",
            }}
         />
         <h2 style={{ color: "#333", marginBottom: "10px" }}>{name}</h2>
         <p style={{ color: "#666", fontStyle: "italic" }}>{occupation}</p>
         <p style={{ color: "#888" }}>📍 {location}</p>

         <div style={{ marginTop: "15px" }}>
            <h4 style={{ color: "#333" }}>Hobbies:</h4>
            <ul
               style={{
                  listStyle: "none",
                  padding: 0,
                  display: "flex",
                  flexWrap: "wrap",
                  justifyContent: "center",
                  gap: "5px",
               }}
            >
               {hobbies.map((hobby, index) => (
                  <li
                     key={index}
                     style={{
                        backgroundColor: "#f0f0f0",
                        padding: "4px 8px",
                        borderRadius: "12px",
                        fontSize: "12px",
                     }}
                  >
                     {hobby}
                  </li>
               ))}
            </ul>
         </div>
      </div>
   );
}

export default PersonalCard;
```

Use the PersonalCard in your App:

```jsx
// src/App.js
import React from "react";
import PersonalCard from "./components/PersonalCard";
import "./App.css";

function App() {
   const people = [
      {
         name: "Sarah Johnson",
         occupation: "Frontend Developer",
         location: "San Francisco, CA",
         hobbies: ["Coding", "Photography", "Hiking"],
         avatar:
            "https://images.unsplash.com/photo-1494790108755-2616b172e8a8?w=100&h=100&fit=crop&crop=face",
      },
      {
         name: "Mike Chen",
         occupation: "UX Designer",
         location: "New York, NY",
         hobbies: ["Design", "Coffee", "Travel"],
         avatar:
            "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&crop=face",
      },
   ];

   return (
      <div className='App'>
         <header style={{ padding: "20px", backgroundColor: "#f5f5f5" }}>
            <h1>Meet Our Team</h1>
            <div
               style={{
                  display: "flex",
                  flexWrap: "wrap",
                  justifyContent: "center",
                  gap: "20px",
               }}
            >
               {people.map((person, index) => (
                  <PersonalCard
                     key={index}
                     name={person.name}
                     occupation={person.occupation}
                     location={person.location}
                     hobbies={person.hobbies}
                     avatar={person.avatar}
                  />
               ))}
            </div>
         </header>
      </div>
   );
}

export default App;
```

## Key Concepts Learned

1. **React Components**: Functions that return JSX
2. **JSX Syntax**: HTML-like syntax in JavaScript
3. **Props**: Data passed to components
4. **Component Composition**: Using components inside other components
5. **Styling**: Inline styles and CSS classes

## Practice Challenge

Create a `ProductCard` component with the following props:

-  `name` (string)
-  `price` (number)
-  `image` (string)
-  `rating` (number 1-5)
-  `inStock` (boolean)

Display the product information with appropriate styling and show "In Stock" or
"Out of Stock" based on the `inStock` prop.

## Next Steps

In the next module, we'll dive deeper into:

-  Component props and prop types
-  Conditional rendering
-  Lists and keys
-  Component composition patterns

## Summary

Congratulations! You've successfully: ✅ Set up a React development
environment  
✅ Created your first React component  
✅ Learned JSX basics  
✅ Built reusable components with props  
✅ Styled components with inline styles

You're now ready to move on to Module 2 where we'll explore more advanced
component concepts!
