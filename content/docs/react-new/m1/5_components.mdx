---
title: "Components"
description:
   "Compose UI with reusable functional components and understand their
   lifecycle."
module: "M1"
lesson: "5"
difficulty: "beginner"
duration: "25"
project_phase: "UI scaffolding"
prerequisites: ["/docs/react-new/m1/4_jsx-iteration"]
learning_objectives:
   - "Define functional components and their structure"
   - "Organise components by responsibility"
   - "Understand component composition"
   - "Plan TaskFlow Pro's component tree"
tags: ["components", "architecture", "composition"]
---

# Components

## Learning Objectives

By the end of this lesson, you will:

-  [ ] Create stateless and stateful functional components.
-  [ ] Compose components to build complex screens.
-  [ ] Plan the component tree for TaskFlow Pro's dashboard.
-  [ ] Recognise when to split components.

## Project Context

TaskFlow Pro relies on modular components for the header, sidebar, task cards,
and various panels. Good boundaries keep the project maintainable.

---

## Functional Components

A functional component is a JavaScript function returning JSX. It can accept
props and contain hooks.

### Basic Example

```tsx
function Button({ children }: { children: React.ReactNode }) {
   return (
      <button className='rounded bg-primary px-3 py-2 text-white'>
         {children}
      </button>
   );
}
```

### Practical Example

```tsx
type DashboardShellProps = {
   sidebar: React.ReactNode;
   header: React.ReactNode;
   children: React.ReactNode;
---

import { Accordions, Accordion } from '@/components/accordion';
};

export function DashboardShell({
   sidebar,
   header,
   children,
}: DashboardShellProps) {
   return (
      <div className='flex min-h-screen'>
         <aside className='hidden w-64 border-r border-border bg-muted/20 p-6 md:block'>
            {sidebar}
         </aside>
         <main className='flex-1 p-6'>
            <header className='mb-6'>{header}</header>
            {children}
         </main>
      </div>
   );
}
```

---

## Composition Patterns

-  **Container + Presentational:** Containers fetch data; presentational
   components render UI.
-  **Layout Components:** Provide consistent structure (e.g., `DashboardShell`).
-  **Slots:** Pass components as props for flexibility (like `sidebar`,
   `header`).

---

## ✅ Best Practices

### 1. Single Responsibility

**Why:** Easier testing and reuse.

```tsx
function TaskListHeader({ total }: { total: number }) {
   return <h2 className='text-lg font-semibold'>Tasks ({total})</h2>;
}
```

### 2. File Organisation

**Why:** Clear paths accelerate navigation.

```text
src/components/
  layout/
    DashboardShell.tsx
  task/
    TaskCard.tsx
```

---

## ❌ Common Mistakes

### 1. Too Many Levels of Nesting

**Problem:** Hard to follow component tree.

**Solution:** Extract nested JSX into their own components.

### 2. Re-export Chaos

**Problem:** Barrel files can hide component sources.

**Solution:** Introduce index files when the module stabilises, not prematurely.

---

## 🔨 Implement in TaskFlow Pro

### Task: Draft Component Tree

1. Create `docs/ui-component-tree.md`.
2. Sketch the hierarchy for the dashboard screen:
   -  Root layout → Header → Sidebar → Main content → Task sections.
3. Note which components handle data vs display only.
4. Update `src/components/layout/DashboardShell.tsx` using the practical example
   above (create directories if needed).
5. Commit with `git commit -am "feat: add dashboard shell"`.

<Accordions type='single' className='mt-4'>
   <Accordion title='Solution Walkthrough'>
      <p>
         Document the dashboard hierarchy and scaffold a reusable shell so every Module 1
         component stays aligned with the TaskFlow Pro UI milestone.
      </p>

      ```md filename="docs/ui-component-tree.md"
      # Dashboard Component Tree

      - <DashboardShell>
        - <Header /> — displays workspace switcher, quick actions, and user menu
        - <div class='flex'>
          - <Sidebar /> — navigation + filter controls
          - <main>
            - <TaskOverview /> — summary metrics
            - <Agenda /> — upcoming tasks (Module 1 Lesson 4)
            - <TaskGrid /> — priority cards (Module 2+)
      ```

      ```tsx filename="src/components/layout/DashboardShell.tsx"
      import type { PropsWithChildren } from 'react';
      import { Sidebar } from '@/components/layout/Sidebar';
      import { Header } from '@/components/layout/Header';

      export function DashboardShell({ children }: PropsWithChildren) {
         return (
            <div className='min-h-screen bg-muted/20 text-foreground'>
               <Header />
               <div className='mx-auto flex w-full max-w-6xl gap-6 px-6 py-8'>
                  <Sidebar />
                  <main className='flex-1 space-y-6'>{children}</main>
               </div>
            </div>
         );
      }
      ```

      ```tsx filename="src/App.tsx"
      import { DashboardShell } from '@/components/layout/DashboardShell';

      export function App() {
         return (
            <DashboardShell>
               {/* Dashboard content continues here */}
            </DashboardShell>
         );
      }
      ```

      ```bash
      git add docs/ui-component-tree.md src/components/layout/DashboardShell.tsx src/App.tsx
      git commit -m "feat: add dashboard shell"
      ```

   </Accordion>
</Accordions>

#### Expected Result

A clear visual map and base layout component make future development smoother.

---

## ✅ Validation Checklist

### Functionality

-  [ ] `DashboardShell` renders sidebar, header, and children properly.

### Code Quality

-  [ ] File paths follow the planned directory structure.
-  [ ] Component responsibilities are documented.

### Understanding

-  [ ] You can explain why composition beats inheritance in React.

### Project Integration

-  [ ] Notes reference the component tree when adding new UI.

---

<div className='mt-8 flex justify-between'>
   <a
      href='/docs/react-new/m1/4_jsx-iteration'
      className='text-sm font-medium text-muted-foreground hover:text-foreground'
   >
      ← JSX Iteration & Conditional Rendering
   </a>
   <a
      href='/docs/react-new/m1/6_component-props'
      className='text-sm font-medium text-primary hover:text-primary/80'
   >
      Next · Component Props
   </a>
</div>
