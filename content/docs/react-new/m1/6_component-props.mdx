---
title: "Component Props"
description: "Pass data and callbacks between components with strong typing."
module: "M1"
lesson: "6"
difficulty: "beginner"
duration: "25"
project_phase: "UI scaffolding"
prerequisites: ["/docs/react-new/m1/5_components"]
learning_objectives:
   - "Define and type component props"
   - "Use destructuring and default values"
   - "Pass callbacks for child-to-parent communication"
   - "Document props for TaskFlow Pro components"
tags: ["props", "typescript", "components"]
---

# Component Props

## Learning Objectives

By the end of this lesson, you will:

-  [ ] Declare prop types using TypeScript interfaces.
-  [ ] Destructure props for readability and defaults.
-  [ ] Pass callbacks upwards to handle events.
-  [ ] Document prop expectations for key TaskFlow Pro components.

## Project Context

Props are how data flows through TaskFlow Pro. Well-typed props prevent bugs and
help components remain reusable.

---

## Declaring Props

### Basic Example

```tsx
type AvatarProps = {
   name: string;
   imageUrl?: string;
};

function Avatar({ name, imageUrl = "/placeholder.png" }: AvatarProps) {
   return (
      <img
         alt={name}
         src={imageUrl}
         className='h-10 w-10 rounded-full border border-border'
      />
   );
}
```

### Practical Example

```tsx
type TaskCardProps = {
   task: Task;
   onStatusChange?: (taskId: string, status: Task["status"]) => void;
};

export function TaskCard({ task, onStatusChange }: TaskCardProps) {
   const handleClick = () => {
      if (!onStatusChange) return;
      onStatusChange(task.id, task.status === "done" ? "todo" : "done");
   };

   return (
      <article className='rounded-lg border bg-card p-4'>
         <header className='flex items-center justify-between'>
            <h3 className='text-base font-medium'>{task.title}</h3>
            <button
               type='button'
               className='text-sm text-primary'
               onClick={handleClick}
            >
               Mark {task.status === "done" ? "To-do" : "Done"}
            </button>
         </header>
      </article>
   );
}
```

---

## ✅ Best Practices

### 1. Document Prop Contracts

**Why:** Future maintainers understand usage quickly.

```md
TaskCard props:

-  `task`: Task object with id/title/status.
-  `onStatusChange`: Optional callback triggered by CTA.
```

### 2. Use Specific Types

**Why:** `Record<string, any>` hides intent; prefer precise shapes.

```ts
type Status = "todo" | "in-progress" | "done";
```

---

## ❌ Common Mistakes

### 1. Mutating Props

**Problem:** Props should be read-only.

```tsx
// Bad
props.task.status = "done";
```

**Solution:** Trigger state updates in the parent.

### 2. Passing Entire App State

**Problem:** Components become tightly coupled.

**Solution:** Send only what's needed or derive data locally.

---

## 🔨 Implement in TaskFlow Pro

### Task: Prop Docs & Typings

1. Create `src/types/task.ts` exporting `Task`, `Project`, and `Priority` types
   if you have not already.
2. Update existing components (`TaskCard`, `Agenda`, etc.) to import from shared
   types.
3. Add prop documentation to `docs/ui-components.md` for Header, Sidebar,
   TaskCard.
4. Commit with `git commit -am "refactor: consolidate task types"`.

#### Expected Result

Components share consistent prop contracts and documentation reflects the
expected shapes.

---

## ✅ Validation Checklist

### Functionality

-  [ ] Components compile using the new shared types.

### Code Quality

-  [ ] Props are destructured with defaults where appropriate.
-  [ ] Optional callbacks are guarded before invocation.

### Understanding

-  [ ] You can explain why props should be immutable.

### Project Integration

-  [ ] Component docs reference the shared type definitions.

---

<div className='mt-8 flex justify-between'>
   <a
      href='/docs/react-new/m1/5_components'
      className='text-sm font-medium text-muted-foreground hover:text-foreground'
   >
      ← Components
   </a>
   <a
      href='/docs/react-new/m1/7_component-lifecycle'
      className='text-sm font-medium text-primary hover:text-primary/80'
   >
      Next · Component Lifecycle
   </a>
</div>
